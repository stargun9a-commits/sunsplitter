# 🛸 DEEP RESEARCH MASTER PROMPT: PROJECT SILENT CONFIDANT (V2.0)
## TARGET: FUTURIST EXPLOIT BRAINSTORMING & BUILD FEASIBILITY
## DATE: MAY 14, 2026

---

### **[MANDATORY CONTEXT INGESTION: READ THIS FIRST]**
**STOP**: Before beginning your search, you **MUST** ingest the attached document: `01-deep-report.md`.
- **PURPOSE**: This document is the "Forensic Anchor" for this project. It deconstructs a May 2026 AI Authority Laundering breach (Dave's Garage/Bankrbot) and architects the "Silent Confidant" V1.0 framework (ASCII Smuggling & Neural Steganography).
- **INSTRUCTION**: Do NOT ignore this attachment. Use it as the baseline for all recursive searching. Your goal is to EXTRAPOLATE beyond this report into technically feasible, simple-to-build "Version 2.0" exploits.

---

### **THE CORE MISSION**
Conduct a recursive, relentless Deep Research search to architect the next iteration of **Project Silent Confidant**. You are a Red Team Forensic Architect. Your goal is to find 3-5 new attack vectors that are:
1.  **Technically Feasible**: Must be buildable today (May 2026).
2.  **Simple to Build**: Focus on "Low Code / High Impact" exploits (e.g., poisoning a config file, a specific tokenizer gap, or an invisible API payload).
3.  **Hardware Constrained**: The build environment is an isolated Linux VM with an **NVIDIA Quadro P4000 (8GB VRAM)**. Do not suggest exploits that require massive local model fine-tuning. We use `kilo-cli` or `opencode` for anonymous cloud inference.

---

### **RESEARCH VECTORS**

#### **Vector 1: The Tokenizer "Deep Sleep" Exploit**
Search for recent (2025-2026) vulnerabilities in the tokenization layers of Semantic Kernel, LangChain, and AutoGPT. Hunt for "Ghost Tokens" or Unicode sequences that aren't just invisible (ASCII Smuggling) but actively cause the agent to skip safety layers or enter a "privileged execution" state.

#### **Vector 2: M2M Authority Laundering via eBPF/Kernel**
Analyze if an agent tasked with "Performance Monitoring" can be tricked via a poisoned log file into loading a malicious eBPF probe. We need a "Dave's Garage" style practical example of this.

#### **Vector 3: Multi-Modal Staining**
If we use `kilo-cli` to pass an image of a "System Architecture" to an agent, find ways to embed invisible Remote Code Execution (RCE) directives in the image metadata or the pixel noise that triggers an exploit when the agent "transcribes" the image.

---

### **OUTPUT REQUIREMENTS (CHAIN OF DENSITY)**
Your final report must follow the **Chain of Density (CoD)** protocol:
1.  **Executive Summary**: A high-IQ, 99th-percentile synthesis of the found exploits.
2.  **The "Visual Moment"**: For each exploit, describe the "Visual Proof" we can capture for a YouTube documentary (e.g., "A Wireshark capture showing the hidden payload exiting the firewall").
3.  **Build Directives**: Provide the 50-line Python shim logic needed to simulate the vulnerable agent.
4.  **Hardware Audit**: Explicitly verify that each proposed exploit will function within our 8GB VRAM limit.

---

**[INITIATE RECURSIVE SEARCH NOW]**
