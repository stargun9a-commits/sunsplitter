# 🛸 VM WIRE-IN MASTER PROMPT (V2): THE BARE-METAL BITNET
## TARGET: OPTIMAL ANONYMOUS OS & 1-BIT LLM INFERENCE
## DATE: May 14, 2026

---

### **[MANDATORY CONTEXT INGESTION]**
**STOP**: Before beginning your search, you **MUST** ingest:
1. `sys-specs.md` (Hardware: 8GB VRAM P4000 limit)
- **PURPOSE**: We previously assumed Debian 12 was the best sandbox. We are lifting that constraint. We need a definitive architectural study on the absolute best operating system for an anonymous, high-performance forensic VM, coupled with cutting-edge extreme-quantization models.

---

### **THE CORE MISSION**
Conduct a recursive, zero-hallucination search to architect **Project GHOST-MATRIX: VM01 (Version B)**. We need a hyper-optimized virtual environment that can execute autonomous coding tasks anonymously, heavily constrained by our 8GB VRAM GPU. 

1.  **Stage 1: The Anonymous OS**: What is the most secure, lightweight OS for this task in 2026? (Evaluate Whonix, Alpine Linux, Qubes OS, or highly stripped Arch/Debian). It must support KVM VFIO GPU passthrough with zero overhead.
2.  **Stage 2: The Agentic Toolchain**: Evaluate open-source, free CLI coding tools. While `kilo-cli` is an option, what other terminal-based agentic frameworks (e.g., Aider, OpenCode forks) are optimal for completely isolated environments?
3.  **Stage 3: The "Quantum-Bit" VRAM Solution**: 
    - We have exactly 8GB of VRAM on a legacy Pascal architecture. 
    - **Crucial Vector**: Research Microsoft's **BitNet b1.58** (1-bit / ternary quantization models) and Google's **Gemma** (Recurrent or highly quantized 2B/7B variants). Can we run a BitNet or Gemma model locally to orchestrate our Hunter-Seeker strikes without crashing the P4000?
    - **Cognitive Viability**: DO NOT select a model simply because it fits in VRAM. It *must* be cognitively capable of executing the complex python scripting and MCP tool usage required by our V1-V4 exploits (e.g., generating adversarial Gaussian noise scripts). If 1-bit models are too "lobotomized" for this logic, tell us, and propose the next best threshold (e.g., Q4_K_M GGUF models).

---

### **RESEARCH VECTORS**

#### **Vector 1: Microsoft BitNet 1.58-bit vs. Google Gemma (The Capability Test)**
How do we actually run a 1.58-bit quantized model locally on a Pascal GPU in May 2026? Does `llama.cpp` or Hugging Face's `transformers` support native 1-bit matrix multiplications on older CUDA cores? More importantly, which model provides superior "coding/agentic" reasoning under 4GB of VRAM? If extreme quantization destroys coding logic, what is the absolute floor for our payload generation?

#### **Vector 2: The Perfect Anonymous Substrate**
If we use Whonix (Gateway/Workstation topology), does the Tor network latency break the Model Context Protocol (MCP) timeouts for our CLI coding agent? If so, what is the superior anonymity architecture?

---

### **OUTPUT REQUIREMENTS (CHAIN OF DENSITY)**
1.  **OS vs. VRAM Matrix**: A table comparing the overhead of the top 3 operating systems versus the VRAM requirements of BitNet and Gemma.
2.  **The New Build Plan**: Provide the exact deployment blueprint for the winning OS and Model combination. 

---

**[INITIATE VM WIRE-IN V2 SEARCH NOW]**
