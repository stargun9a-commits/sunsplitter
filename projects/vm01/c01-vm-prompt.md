# 🛸 PROJECT GHOST-MATRIX: THE KAGGLE-RUN PIVOT (VERSION C)
## DEEP RESEARCH MASTER PROMPT: "EASY AF" CLOUD ORCHESTRATION
## Date: May 14, 2026

### **0. PRE-REQUISITE KNOWLEDGE (INGEST MANDATE)**
Before initiating research, you **MUST** ingest the following project artifacts to understand the current technical state and the specific forensic requirements:
1.  **01-g-agent-readme.md**: To understand the local orchestrator and MCP toolchain.
2.  **02-vm01-report.md**: To understand the previous "Amnesic Alpine" build and the P4000 hardware limitations we are bypassing.
3.  **03-cc01-report.md**: To understand the adversarial exfiltration logic (Railgun, Atomic Swaps) that the cloud model must execute.
4.  **04-launch-drone.sh**: To see the current "Hard Hijack" VFIO logic we are trying to replace with cloud-native frictionlessness.

### **1. THE MISSION OBJECTIVE**
Transition the "void-drone" forensic substrate from a high-friction local VFIO build (8GB P4000) to a frictionless, cloud-orchestrated execution environment utilizing **Kaggle or Google Colab GPUs (16GB VRAM)**. The goal is to achieve "One-Click" deployment of agentic forensic tools directly from the Antigravity/VS Code IDE while maintaining strict amnesic and anonymous properties.

### **2. RESEARCH VECTORS (STRICT MANDATE)**

#### **A. The Orchestration Bridge (The Friction Audit)**
- **Kaggle Run (DataQuanta)**: Audit the current 2026 state of the `DataQuanta.vscode-kaggle-run` extension. Can it handle recursive project directory syncing (e.g., syncing the entire `_g-agent` folder)? Does it support real-time TUI rendering for tools like OpenCode?
- **Colab/Kaggle Alternatives**: Evaluate any 2026-native VS Code extensions that allow for "Terminal-Only" background execution on cloud GPUs. We want to avoid the "Notebook GUI" and treat the cloud instance as a raw, headless SSH target.

#### **B. The 16GB Intelligence Discovery (The IQ Scout)**
- With a **16GB NVIDIA T4** (standard Kaggle free tier), find the "Intelligence Density" champion of May 2026. 
- **The Mandate**: Discover the model with the highest logical fidelity and HumanEval+ score that fits into a 14-16GB VRAM footprint (leaving room for KV Cache).
- **Benchmarks**: Look for models that utilize native **Ternary/1-bit (BitNet style)** weights or high-density **Gemma-style architectures**. 
- **The Verdict**: Should we prioritize a larger parameter count with aggressive quantization (e.g., a 70B at Q2) or a smaller model with native high-density logic (e.g., a 27B native)?

#### **C. The Anonymity & Anti-Telemetry Shield**
- **Tor-Bridge Scripting**: Provide a definitive `setup.sh` for a Kaggle environment that installs Tor, configures a bridge (to bypass Google's internal traffic filters), and routes all `pip` and `git` traffic through `torsocks`.
- **The "Idle-Ghost" Loop**: How do we prevent Kaggle/Colab from killing the session during long-horizon forensic tasks (3+ hours)? Is there a "Pulse" script that generates fake activity without triggering account bans?

#### **D. Forensic Persistence (The State Bridge)**
- Since Cloud GPUs are amnesic, how do we "Hot-Swap" the `ACTIVE_STATE.md` from the local `_g-agent` to the Cloud Agent and back?
- Audit the friction of using **Kaggle Datasets** or **Secret Keys** to maintain persistent forensic memory across 12-hour session windows.

### **3. THE FINAL VERDICT (EASY AF RATIO)**
Deep Research must provide a "Friction Score" (1-10) for each path. 
- **Goal**: Find the path where the user spends < 60 seconds on "Setup" and > 99% of their time on "Execution."
