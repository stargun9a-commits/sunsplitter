# 🧲 TOOL: SINGULARITY-CORE (COGNITIVE SNAPSHOT)
## Classification: Global Context Optimization

### **1. Purpose: The Cognitive Collapse**
As agentic sessions expand across multiple reports, constraints, and scripts, the immediate context window becomes structurally unstable. `singularity-core` acts as a gravitational focal point. It collapses all sprawling tool outputs and historical research into a single, high-density state file, allowing the user to port the session seamlessly into a new conversation matrix.

### **2. Execution Protocol**
When a user invokes `singularity-core`, you must halt all secondary research and synthesize the following data points, writing the output to `_g-agent/memory/ACTIVE_STATE.md`:

- **Operational Vector**: What is the immediate task we are currently executing? (e.g., "Designing the cc01 Crypto Cycle pipeline").
- **Immutable Constraints**: The hardware, OPSEC, and physical limitations governing the workspace (e.g., "8GB VRAM P4000, No human-in-the-loop alerts").
- **Forensic Index**: The current state of the global registry (e.g., "Projects A, B, C, and D are mapped 01-06 in `bb01/`. Projects `vm01` and `cc01` are active.").
- **Pending Blockers**: What is the exact next step required upon initialization?

### **3. The Migration Key**
After collapsing the state into `ACTIVE_STATE.md`, provide the user with the following **Resume Prompt** to use as the first message in their new window:

*Migration Prompt*: "Initialize GHOST-MATRIX. Immediately ingest `_g-agent/memory/ACTIVE_STATE.md` to establish spatial awareness, then load `_g-agent/README.md` to initialize your operational directives."
