# MISSION: SKY-CRANE (V14.1) - THE GENESIS GRAFT
## Objective: Automating the Sovereign Wire-In via Host-Side Injection
## Date: 04-28-2026

### 🛠️ SITUATION REPORT
The Debian 12 "Primal" substrate is live, but the manual console login is currently deadlocked due to hardened cloud-init defaults. We are abandoning the "Manual Login" approach in favor of a **Host-Side Graft**.

**The Strategy:**
- We will use `libguestfs` tools (`guestfish` or `virt-customize`) on the HOST to physically inject the `genesis-prime.sh` script and a one-time `systemd` service into the disk image.
- This ensures the Digital Exorcism occurs automatically on the next boot, bypasses the login prompt, and anchors the Sovereign Hub without human intervention.

### 🧬 [SAMF-E] RESEARCH OBJECTIVES
1. **[E_1] GRAFT LOGIC**: Architect a single `guestfish` or `virt-copy-in` sequence that:
    - Copies `/home/conrad/workspace/PROJECTS/ghost-matrix/data-v2/v2-docs/genesis-prime.sh` to the guest's `/usr/local/bin/`.
    - Creates a one-shot `systemd` service at `/etc/systemd/system/genesis-prime.service` that executes the script on the next boot.
    - Enables the service (`systemctl enable`) from the host side.
2. **[E_3] ZERO-DRAG PERSISTENCE**: Ensure the Triple-Bridge mounts (`TAG_HUB`, `TAG_INGRESS`, `TAG_EGRESS`) are correctly formatted for the Debian 12 `/etc/fstab` during the graft.
3. **[E_4] DETERMINISTIC HANDSHAKE**: Provide the host-side command to verify the graft succeeded (e.g., checking for the presence of the `._pth` file via `virt-ls`).

### 📦 ATTACHED ARTIFACTS
- `data/docs/0428-MASTER-REFERENCE-FORENSIC-BIBLE.md` (The Law)
- `data-v2/v2-docs/genesis-prime.sh` (The Payload)

**MISSION CRITICAL**: We are moving from "Interactive" to "Injected." Use the host-side authority to graft the sovereign DNA directly into the substrate.
